# Operator Memory

Operator memory is intentionally separate from global deployment patterns. Use this directory for DS preferences, approval tendencies, or communication style constraints that should not silently leak into the canonical skill system.
