# Expected Structure

A strong output contains:

- a `DATA-CONNECTION-STATUS.md` with real blocker tracking
- a `PIPELINE-BUILD-STATUS.md` showing ontology-ready datasets
- a `SECURITY-AUDIT.md` with explicit exploit paths
- a `QA-REPORT.md` with readiness evidence and at least one warning path
